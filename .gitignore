# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# Test & Coverage
coverage/
.nyc_output/

# IDE & Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test
coverage/
.nyc_output/

# Turbo
.turbo/

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# Vite
.vite/

# Readonly directory (not for upload)
.readonly/

# Supabase — 有意整个目录都不跟踪。迁移脚本/config.toml/seed.sql 的权威版本迁到了
# 独立仓库 HiWmsSupabase（DBA 团队管理，https://github.com/AaronLucas/HiWmsSupabase），
# 与本仓库没有 git 层面关联（无 submodule）。本地需要时用
# `bash scripts/sync-db-migrations.sh` 同步到这里；CI 的 DB 集成测试 job
# （.github/workflows/db-integration.yml）通过只读 Deploy Key 单独 checkout 该仓库。
# 这条解决了 docs/03-database/REPOSITORY_ROADMAP.md §「剩余缺口清单」CRITICAL 第 2 项
# 此前记录的"是否开始把这些文件提交入库"暂缓决策（2026-07-20 确认：不入库，改用独立仓库）。
supabase/*

# Claude local artifacts (worktrees, local settings)
.claude/worktrees/
.claude/settings.local.json

# Backup directories (timestamped)
supabase/migrations.backup.*
