{
  "name": "wms-workflow-engine",
  "version": "1.0.0",
  "description": "Enhanced workflow engine with auto-retry, circuit breaker, and type-safe workflow engine",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node src/test.ts",
    "test:retry": "ts-node src/test.ts",
    "lint": "tsc --noEmit",
    "ci": "npm run lint && npm run test"
  },
  "keywords": [
    "workflow",
    "retry",
    "circuit-breaker",
    "orchestration"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "minimatch": "^9.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.1.6"
  }
}